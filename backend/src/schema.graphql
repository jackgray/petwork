# import * from './generated/prisma.graphql'

type Mutation {
	createDog(
		name: String!
		age: Int
		breed: String
		gender: String
		image: String
		largeImage: String
		location: String
		shelter: String
	): Dog!
}

type Query {
	dogs: [Dog]
}
