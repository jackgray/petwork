# import * from './generated/prisma.graphql'

type Mutation {
	createDog(
		name: String!
		age: Int
		breed: String
		location: String
		shelter: String
		image: String
	): Dog!
}

type Query {
	dogs: [Dog]
}
